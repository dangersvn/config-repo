# gateway-service.yml (default config for gateway service)
server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          # Ensures service IDs are converted to lowercase for consistent routing
          lower-case-service-id: true