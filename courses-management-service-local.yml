logging:
  level:
    com.netflix.discovery: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: refresh,info

course:
  greeting: "Hello from local Config!"
  welcomeMessage: "Welcome to local Course Management - Initial Version"
  maxStudents: 160